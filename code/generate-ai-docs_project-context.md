Act as an expert Full-stack Architect. Based on my current project structure, codebase, and configuration files, please generate a comprehensive GEMINI.md and CLAUDE.md documentation file (two different files with same content). This file will serve as a persistent context guide for AI collaborators.

Please follow this structure strictly:**

Project Overview: A brief summary of what the app does, the core user flow, and the primary stack.

Technical Stack: A table for Backend and Frontend listing key libraries, frameworks, and tools used.

Workflow & Rules:

How to run commands (e.g., Docker, scripts, local dev).

Coding standards (naming conventions, patterns).

Documentation and commit requirements (keep them concise and AI-reference free).

Design System & UI (if applicable): Specific rules for styling (Tailwind, CSS-in-JS), component usage, iconography, and typography.

Architecture: A directory tree overview explaining where core logic, models, and UI components reside.

Lessons Learned: A placeholder section for specific project "gotchas" or recurring bug fixes discovered during development.

Instructions for the output:

Be precise and technical.

Use the same clean Markdown formatting as the SAPORIUM example (bolding for emphasis, horizontal rules, tables).

Analyze my specific settings (like package.json, requirements.txt, pyproject.toml, or next.config.js) to ensure the stack table is 100% accurate.

Tone: Professional, structured, and developer-centric.